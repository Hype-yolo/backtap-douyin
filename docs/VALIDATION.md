# 验证范围

验证日期：2026-09-19。状态：实验性自建项目，无公共解析实例。

## 已验证

- 18 项本地单元测试：输入提取、域名限制、重定向限制、目标视频 ID 匹配、媒体地址校验、认证、错误信息脱敏、公共额度持久化及限流。
- macOS“快捷指令”导入了同一生成器的 25 个动作，确认剪贴板输入、正则匹配、变量、条件和下载动作被系统正确识别。
- Ubuntu 24.04、Node.js 20.20.2 上，以独立用户及 Chromium 沙箱取得公开目标视频的信息。
- 受限 systemd 服务真实解析成功；单请求样本约 32 秒，服务 cgroup 峰值约 421 MiB，未取消资源限制。
- 在服务器之外，用返回的临时地址完整下载了一份 MP4；ffprobe 验证：1080×1920、H.264 视频、AAC 音频、33.733734 秒、9,401,019 字节。
- 试部署已按项目范围调整停止；测试时检查的已有三个常驻产品服务保持 active。生产环境数据和既有站点路由未被替换。

## 尚未验证 / 不承诺

- iPhone 完整的“轻点背面 → 剪贴板 → 下载 → 照片”端到端验收。
- 多人长期并发、持续成功率、所有作品、所有地区网络和所有 iOS 版本。
- 固定画质、去水印、平台接口长期兼容、用户不需要任何权限确认。

发布安装包只表示文件已签名并可供添加，不等于每台 iPhone 都已验证成功。请按安装教程先手动运行并检查真实相册文件，再绑定轻点背面。
